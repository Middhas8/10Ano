#include <iostream>// Biblioteca de entrada e saída, para usar cin e cout
#include <string>// Biblioteca de string, que é uma estrutura de dados para armazenar texto
#include <vector>// Biblioteca de vetor, que é uma estrutura de dados dinâmica
#include <locale.h>// Biblioteca que permite usar acentos
#include <cstdlib>// Biblioteca que permite usar funções de conversão, como atoi, atof, etc.
#include <time.h>// Biblioteca que permite usar funções de tempo, como time, clock, etc.
#include <cmath>// Biblioteca que permite usar funções matemáticas, como sqrt, pow, etc.
#include <algorithm>// Biblioteca que permite usar funções de algoritmos, como sort, find, etc.
#include <map>// Biblioteca que permite usar a estrutura de dados map, que é um dicionário
#include <set>// Biblioteca que permite usar a estrutura de dados set, que é um conjunto
using namespace std;
int main()
{
	ios_base::sync_with_stdio(false);     cin.tie(NULL);//Acelera a entrada e saída de dados
	setlocale(LC_ALL, "Portuguese");// Permite usar acentos, use se precisar
	  return 0;
}